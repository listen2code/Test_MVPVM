package ${packageName}.view;

import ${packageName}.model.viewmodel.I${activityClass}ViewModel;

public interface I${activityClass}View {

    void updateView(I${activityClass}ViewModel viewModel);
}
